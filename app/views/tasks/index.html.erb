<div class="container">
  <h1 class="text-center">Tasks</h1>

  <ul class="row text-center">
    <% @tasks.each do |task|  %>
    <div class="col alert alert-primary text-center m-2"><%= link_to "#{task.title} : #{task.details}", task_path(task) %></div>
    <% end %>
  </ul>
</div>
